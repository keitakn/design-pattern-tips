{
  "compilerOptions": {
    // モジュール形式
    "module": "commonjs",
    // モジュールの解決方法
    "moduleResolution": "node",
    // コンパイル後のターゲット（'es3', 'es5', 'es2015', 'es2016', 'es2017'）
    // 何も指定しない場合のデフォルト値は'es3'の模様
    "target": "es2017",
    // SourceMapを出力する
    "inlineSourceMap": true,
    "inlineSources": true,
    // 出力先のディレクトリ
    "outDir": "dist",
    // コンパイル時にコメントを削除する
    "removeComments": true,
    // 暗黙のany型を禁止する
    // Null安全かどうかチェックする（一部抜け道はありなので完璧ではない）
    // use strict; を自動で追加
    "strict": true,
    // 未使用なローカル変数はエラーにする
    "noUnusedLocals": true,
    // 未使用のパラメーターをエラーにする
    "noUnusedParameters": true,
    // 到達しえないコードがある場合はエラーにする
    "allowUnreachableCode": false,
    // 到達しえないラベルがある場合はエラーにする
    "allowUnusedLabels": false,
    // case 文 の フォールスルーをエラーにする
    "noFallthroughCasesInSwitch": true,
    // 暗黙的な undefined を返す事を禁止する
    "noImplicitReturns": true,
    // experimentalDecoratorsを有効にする
    "experimentalDecorators": true,
    // ファイル名の大文字小文字の違いをエラーにする
    "forceConsistentCasingInFileNames": true,
    // 改行コード
    "newLine": "LF",
    "lib": [
      "es2015",
      "es2016",
      "es2017"
    ],
    "types": [
      "node",
      "jest"
    ]
  },
  "exclude": [
    "node_modules"
  ]
}
